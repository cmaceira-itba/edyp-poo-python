[project]
name = "edyp-poo-python"
version = "0.1.0"
description = "Material de referencia para POO en Python — ITBA EDyP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material>=9.7.5",
    "pytest>=9.0.2",
]
